/** Minimum pause between marketing dispatch batches (5 seconds). */
export const WHATSAPP_MARKETING_BATCH_DELAY_MS_MIN = 5 * 1000;

/** Maximum pause between marketing dispatch batches (20 minutes). */
export const WHATSAPP_MARKETING_BATCH_DELAY_MS_MAX = 20 * 60 * 1000;
